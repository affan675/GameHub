// Shared utilities – currently just ensures dynamic content loads.
// Additional global helpers can go here.
document.addEventListener('DOMContentLoaded', () => {
    console.log('GameHub ready!');
});